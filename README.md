# 1st Project

A minimal, containerized starting point for Alloy development sessions.

## Run locally

```sh
docker compose -f docker-compose.alloy.yaml up -d
```

The frontend listens on [http://localhost:3000](http://localhost:3000). A health check is available at
`http://localhost:3000/health`.
